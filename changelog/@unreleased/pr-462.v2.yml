type: fix
fix:
  description: Dialogue projects now depend on `dialogue-target` 1.50.0 as this is
    more defensive to the possibility that consumers might have mixed versions of
    `com.palantir.dialogue` jars (specifically a too low `dialogue-serde` jar).
  links:
  - https://github.com/palantir/gradle-conjure/pull/462
